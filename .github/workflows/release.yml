name: Release new version of Application

on:
    push:
        branches: [ "dev" ]

jobs:
    start-crafts:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Start Triggering other jobs!"
            - uses: roomonmoon/Syncogram2/.github/workflows/linux.yml@dev